"use client";

export default () => {
  return (
    <div
      // tailwind classnames to make div 600px wide if desktop and full width of parent if mobile and margin bottom 1rem
      className="w-full md:w-1/4 mb-9"
      id="commandbar-inline-root"
    />
  );
};
